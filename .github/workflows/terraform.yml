name: Terraform CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.7

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      
      - name: Check secret content (debug)
        run: echo "${{ secrets.AZURE_CREDENTIALS }}" | head -n 1
    

      - name: Terraform Init
        run: terraform -chdir=infra/terraform init -input=false -no-color

      - name: Terraform Plan
        run: terraform -chdir=infra/terraform plan -input=false -no-color -lock=false -out=tfplan

      #- name: Terraform Apply
      #  if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      #  run: terraform apply -input=false -no-color tfplan -chdir=infra/terraform
